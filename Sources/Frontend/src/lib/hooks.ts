/**
 * @deprecated Import from "@/hooks" instead.
 * This file is a compatibility shim — it re-exports everything from the new location.
 * Kept to avoid breaking existing imports during the migration.
 */
export * from "@/hooks";
