# =================================================================
# RAG MODULE CONFIGURATION
# Profile: rag (dùng khi chuyển sang PostgreSQL + PGVector)
# Chạy với: java -jar app.jar --spring.profiles.active=rag
# =================================================================

spring.application.name=SmartCity-RAG

# ─── PostgreSQL + PGVector ───────────────────────────────────────
# [SECURITY FIX] Bỏ hardcode credentials — set biến môi trường khi chạy
# Windows: $env:DB_HOST="localhost"; $env:DB_PASSWORD="yourpassword"
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:ragdb}
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# HikariCP — Connection Pool cho PostgreSQL
spring.datasource.hikari.maximum-pool-size=15
spring.datasource.hikari.minimum-idle=3
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.leak-detection-threshold=60000
spring.datasource.hikari.pool-name=SmartCity-RAG-Pool

# ─── JPA / Hibernate ─────────────────────────────────────────────
# [SECURITY FIX] ddl-auto=update nguy hiểm cho production (Hibernate có thể tự ý xóa cột)
# Dùng Flyway để quản lý migration thay thế
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=false

# ─── Flyway Migration ────────────────────────────────────────────
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# ─── Spring AI (Uncomment khi có API key) ────────────────────────
# spring.ai.openai.api-key=${OPENAI_API_KEY}
# spring.ai.openai.embedding.model=text-embedding-3-small
# spring.ai.openai.chat.model=gpt-4o-mini

# ─── Redis (Uncomment khi có Redis) ──────────────────────────────
# spring.data.redis.host=localhost
# spring.data.redis.port=6379
# spring.cache.type=redis

# ─── Actuator ────────────────────────────────────────────────────
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always
# [FIX] key đúng cho Spring Boot 3.x (key cũ bị deprecated)
management.prometheus.metrics.export.enabled=true
management.metrics.tags.application=${spring.application.name}

# Cache TTL — ngăn dữ liệu thời tiết cũ được giữ quá lâu trong bộ nhớ
spring.cache.caffeine.spec=maximumSize=500,expireAfterWrite=15m

# ─── Async Thread Pool ───────────────────────────────────────────
# Virtual Threads (Java 21) — tự động dùng cho @Async
spring.threads.virtual.enabled=true

# ─── Logging ─────────────────────────────────────────────────────
logging.level.com.example.smartcity.rag=DEBUG
logging.level.org.springframework.web=INFO
# Thêm MDC parameters vào Log format
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level [%X{requestId:-SYSTEM}] [%X{userId:-SYS}] [%thread] %logger{36} - %msg%n

# ─── Resilience4j Config ─────────────────────────────────────────
resilience4j.circuitbreaker.configs.default.registerHealthIndicator=true
resilience4j.circuitbreaker.configs.default.slidingWindowSize=10
resilience4j.circuitbreaker.configs.default.failureRateThreshold=50
resilience4j.circuitbreaker.configs.default.waitDurationInOpenState=60s
resilience4j.circuitbreaker.configs.default.permittedNumberOfCallsInHalfOpenState=3
resilience4j.circuitbreaker.configs.default.minimumNumberOfCalls=5
resilience4j.circuitbreaker.configs.default.automaticTransitionFromOpenToHalfOpenEnabled=true

resilience4j.circuitbreaker.instances.groq.baseConfig=default
resilience4j.circuitbreaker.instances.gemini.baseConfig=default

